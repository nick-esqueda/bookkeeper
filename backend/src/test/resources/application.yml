spring:
  datasource:
    url: jdbc:tc:mysql:8.0.36:///test_database?TC_REUSABLE=true
    username: user
    password: password
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: update
